apiVersion: apps/v1
kind: Deployment
metadata:
  name: jussun-api
  namespace: production
  labels:
    app: jussun-api
spec:
  selector:
    matchLabels:
      app: jussun-api
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: jussun-api
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: jussun-api
          image: ghcr.io/champalab/jussun-api:4b2bb9484ea864cb655c982b29b034a57a52c95c
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 500m
              memory: 100Mi
            limits:
              cpu: 2000m
              memory: 2048Mi
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 5
            successThreshold: 1
            failureThreshold: 3
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
            periodSeconds: 10
          envFrom:
            - configMapRef:
                name: jussun-api-config
            - secretRef:
                name: database-parking-hub
            - secretRef:
                name: jwt-secret
          ports:
            - containerPort: 8080
              name: http
          volumeMounts:
            - name: localtime
              mountPath: /etc/localtime
            - name: vol-jussun
              mountPath: /app/logs
              subPath: vol-jussun/logs-prod
            - name: vol-jussun
              mountPath: /app/uploads
              subPath: vol-jussun/uploads-prod
      volumes:
        - name: localtime
          hostPath:
            path: /usr/share/zoneinfo/Asia/Bangkok
        - name: vol-jussun
          nfs:
            path: /mnt/k8s
            server: gfs-01
      restartPolicy: Always
